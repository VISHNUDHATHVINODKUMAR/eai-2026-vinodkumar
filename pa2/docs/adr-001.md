# ADR-001 — <the decision, as a short noun phrase>

<!--
PA2 architecture decision record. Required: a public test checks that the
four `##` headings below are present, and the manual 30% of this assignment
is the quality of what you write under them.

Keep the headings exactly as they are. One page is enough.

The question this ADR is really asking you:

    A durable queue, publisher confirms, manual ack and a correlationId are
    four separate decisions, each with a real alternative that looks almost
    as good until the broker actually dies. Which one bit you first, and
    what would you have shipped if you had skipped it?

Write it after you have finished the code and run the demonstrations, while
the annoyance from watching messages disappear is still fresh.
-->

**Status:** accepted
**Date:** YYYY-MM-DD

## Context

<!--
What forced each decision? Be specific — name the header, the flag, the
container you killed. "Messages can get lost" is not context;
"publishing 5 messages with X-Publish-Mode: fire-and-forget and then
`docker restart pa2-rabbitmq` made 3 of them vanish, with the HTTP client
having already received a 202 for all 5" is.

Four real decision points this assignment forces, worth naming if you hit
them:

  - durable queue vs. not — what actually happens to a non-durable queue's
    contents across `docker restart pa2-rabbitmq`, and did you check, or
    did you take the README's word for it?
  - publisher confirms vs. not — what does a 202 from POST /publish
    actually promise the caller, under each mode, and did the demonstration
    change what you believed it promised?
  - manual ack vs. auto-ack — what happens to a message that is mid-write
    to received.jsonl when the consumer process dies, under each?
  - what correlationId propagation actually requires — is it enough that
    the value shows up in the consumer's log line, or does something have
    to be true about it at every hop (HTTP header -> AMQP property ->
    JSONL field) for that to mean anything?
-->

## Decision

<!--
What you built, specifically enough that I could find it in your code. Name
the function (connectProducerChannel, publishMessage, connectConsumerChannel,
handleMessage) and what it actually does — not what the README asked for.

Include the decisions that are easy to wave at instead of stating: what
"durable: true" is set on, what "persistent" is set to and in which mode,
where the manual ack sits relative to the write to received.jsonl, and
whether msg.properties.correlationId is trusted as-is or checked.
-->

## Alternatives considered

<!--
At least one real alternative with the reason you rejected it. Real options
here include: acking before writing to received.jsonl instead of after;
using the message body for the correlationId instead of the AMQP
`correlationId` property; auto-ack with a lower prefetch instead of manual
ack; skipping durability altogether since this is "just an assignment";
keeping the guest/guest credentials instead of a named RabbitMQ user; using
`channel.publish()`'s return value as your only backpressure signal instead
of the confirm callback.

That last one is not a throwaway option — it is what most naive amqplib code
actually does, and it is close enough to correct that it is worth explaining
why it still is not.
-->

## Consequences

<!--
What is now easier, what is now harder, and what breaks first.

Concretely: what happens to this system if the consumer is slow enough that
the queue backs up for an hour? What happens if two consumer instances run
against the same queue at once? What happens if a message's JSON body is
100MB? What happens if RabbitMQ's disk fills up? Name a cost you accepted —
a consequences section with no downside in it is not credible.
-->
